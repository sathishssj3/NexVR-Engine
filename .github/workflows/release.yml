# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: CI - Build, Test, and Analyze

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: windows-latest
    permissions:
      contents: read
      # Needed for the GitHub CodeQL action
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        configuration: [Debug, Release]

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Setup MSVC & CMake
      uses: ilammy/msvc-dev-cmd@v1.13.0

    - name: Download Official DXC (for SPIR-V support)
      shell: cmd
      run: |
        curl.exe -L https://github.com/microsoft/DirectXShaderCompiler/releases/download/v1.7.2308/dxc_2023_08_14.zip -o dxc.zip
        7z.exe x dxc.zip -odxc -y

    - name: Setup Vulkan SDK
      shell: pwsh
      run: |
        $ErrorActionPreference = 'Stop'
        $VulkanURL = "https://sdk.lunarg.com/sdk/download/latest/windows/vulkan-sdk.exe"
        Write-Host "Downloading Vulkan SDK..."
        Invoke-WebRequest -Uri $VulkanURL -OutFile VulkanSDK.exe
        Write-Host "Installing Vulkan SDK..."
        Start-Process -FilePath ".\VulkanSDK.exe" -ArgumentList "--root", "C:\VulkanSDK", "--accept-licenses", "--default-answer", "--confirm-command", "install" -Wait -NoNewWindow
        Write-Host "Configuring Environment..."
        echo "VULKAN_SDK=C:\VulkanSDK" >> $env:GITHUB_ENV
        echo "C:\VulkanSDK\Bin" >> $env:GITHUB_PATH

    - name: Configure CMake
      run: cmake -B build -S . -A x64 -DDXC_EXECUTABLE="${{ github.workspace }}/dxc/bin/x64/dxc.exe" -DBUILD_TESTS=ON

    - name: Build
      shell: pwsh
      run: |
        $ErrorActionPreference = 'Stop'
        cmake --build build --config ${{ matrix.configuration }} -- /maxcpucount
        if ($LASTEXITCODE -ne 0) {
          Write-Error "Build failed!"
          exit 1
        }

    - name: Run Unit Tests
      shell: pwsh
      run: |
        $ErrorActionPreference = 'Stop'
        # Enable testing and run tests
        ctest --test-dir build --output-on-failure -C ${{ matrix.configuration }}
        if ($LASTEXITCODE -ne 0) {
          Write-Error "Tests failed!"
          exit 1
        }

    - name: Run Cppcheck Static Analysis
      shell: pwsh
      run: |
        # Install cppcheck if not present (windows-latest runners may not have it)
        if (-not (Get-Command cppcheck -ErrorAction SilentlyContinue)) {
          choco install cppcheck -y --no-progress | Out-Null
          Import-Module "$env:ChocolateyInstall\helpers\chocolateyProfile.psm1"
          refreshenv
        }
        # Choco's cppcheck build has a hardcoded FILESDIR that doesn't exist on
        # GitHub runners.  Locate the real cfg/ directory next to the binary.
        $cppcheckBin = (Get-Command cppcheck -ErrorAction SilentlyContinue).Source
        if (-not $cppcheckBin) { Write-Host "cppcheck not found, skipping"; exit 0 }
        $cfgDir = Join-Path (Split-Path $cppcheckBin) "cfg"
        if (-not (Test-Path $cfgDir)) {
          # Try the choco lib path
          $cfgDir = (Get-ChildItem "C:\ProgramData\chocolatey\lib\cppcheck\tools" -Recurse -Directory -Filter "cfg" -ErrorAction SilentlyContinue | Select-Object -First 1).FullName
        }
        $cfgArgs = @()
        if ($cfgDir -and (Test-Path $cfgDir)) {
          Write-Host "Found cppcheck cfg at: $cfgDir"
          $cfgArgs = @("--library-cfg-dir=$cfgDir")
        } else {
          Write-Host "Warning: cppcheck cfg directory not found - std.cfg may be missing"
        }
        cppcheck --error-exitcode=1 --enable=warning,style,performance `
          --suppress=missingIncludeSystem `
          --suppress=unmatchedSuppression `
          --inline-suppr `
          @cfgArgs `
          -I src `
          src/ 2>&1
        if ($LASTEXITCODE -ne 0) { exit 1 }

    # - name: Run Clang-Tidy
    #   uses: owner-needed/clang-tidy-action@v1
    #   with:
    #     clang-tidy-bin: clang-tidy
    #     path: build
    #     clang-tidy-warnings-as-errors: '*'
    #     header-filter: '.*'
    #     format-style: file

    - name: Run CodeQL Analysis
      uses: github/codeql-action/init@v3
      with:
        languages: cpp

    - name: CodeQL Autobuild
      uses: github/codeql-action/autobuild@v3

    - name: CodeQL Perform Analysis
      uses: github/codeql-action/analyze@v3
      with:
        category: "/language:cpp"

    - name: Upload Test Results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: test-results-${{ matrix.configuration }}
        path: |
          **/TestResult*.xml
          **/test_results*.xml
          **/TEST-*.xml
        retention-days: 7

  # Keep the original release job for when releases are published
  release:
    needs: build-and-test
    runs-on: windows-latest
    if: github.event_name == 'release'
    permissions:
      contents: write
      issues: write

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Setup MSVC & CMake
      uses: ilammy/msvc-dev-cmd@v1.13.0

    - name: Download Official DXC (for SPIR-V support)
      shell: cmd
      run: |
        curl.exe -L https://github.com/microsoft/DirectXShaderCompiler/releases/download/v1.7.2308/dxc_2023_08_14.zip -o dxc.zip
        7z.exe x dxc.zip -odxc -y

    - name: Setup Vulkan SDK
      shell: pwsh
      run: |
        $ErrorActionPreference = 'Stop'
        $VulkanURL = "https://sdk.lunarg.com/sdk/download/latest/windows/vulkan-sdk.exe"
        Write-Host "Downloading Vulkan SDK..."
        Invoke-WebRequest -Uri $VulkanURL -OutFile VulkanSDK.exe
        Write-Host "Installing Vulkan SDK..."
        Start-Process -FilePath ".\VulkanSDK.exe" -ArgumentList "--root", "C:\VulkanSDK", "--accept-licenses", "--default-answer", "--confirm-command", "install" -Wait -NoNewWindow
        Write-Host "Configuring Environment..."
        echo "VULKAN_SDK=C:\VulkanSDK" >> $env:GITHUB_ENV
        echo "C:\VulkanSDK\Bin" >> $env:GITHUB_PATH

    - name: Decode Code Signing Certificate
      shell: pwsh
      env:
        CERT_BASE64: ${{ secrets.SIGN_CERT_PATH }}
      run: |
        if ([string]::IsNullOrWhiteSpace($env:CERT_BASE64)) {
          Write-Host "No certificate secret found. Skipping decoding."
          exit 0
        }

        # Remove absolutely everything that is not a valid Base64 character
        $cleanBase64 = $env:CERT_BASE64 -replace '[^A-Za-z0-9+/=]', ''

        # Ensure the string length is a multiple of 4 (add missing padding)
        $pad = $cleanBase64.Length % 4
        if ($pad -gt 0) {
          Write-Host "Warning: Missing padding detected. Attempting to auto-fix..."
          $cleanBase64 += "=" * (4 - $pad)
        }

        try {
          $bytes = [Convert]::FromBase64String($cleanBase64)
          [IO.File]::WriteAllBytes("${{ github.workspace }}\cert.pfx", $bytes)
          echo "SIGN_CERT_PATH=${{ github.workspace }}\cert.pfx" >> $env:GITHUB_ENV
          echo "SIGN_CERT_PASS=${{ secrets.SIGN_CERT_PASS }}" >> $env:GITHUB_ENV
          Write-Host "Successfully decoded certificate."
        } catch {
          Write-Host "ERROR: The Base64 string is still completely invalid. You must re-copy it perfectly."
          exit 1
        }

    - name: Configure CMake
      run: cmake -B build -S . -A x64 -DDXC_EXECUTABLE="${{ github.workspace }}/dxc/bin/x64/dxc.exe"

    - name: Build C++ Components (DLL & CLI)
      shell: pwsh
      run: |
        $ErrorActionPreference = 'SilentlyContinue'
        cmake --build build --config Release > build.log 2>&1
        if ($LASTEXITCODE -ne 0) {
          Write-Host "Build failed! Pushing log to repo..."
          $LogTail = Get-Content build.log -Tail 300 | Out-String
          Write-Host "=== ERROR PREVIEW ==="
          Write-Host $LogTail
          Write-Host "====================="
          Set-Content -Path build_crash.log -Value $LogTail
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add build_crash.log
          git commit -m "[bot] Upload build crash log for analysis" || true
          git pull --rebase origin main || true
          git push origin HEAD:main || true
          exit 1
        }

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: Install Launcher Dependencies
      working-directory: ./launcher
      run: npm install

    - name: Build and Package Electron App
      working-directory: ./launcher
      # This triggers vite build, tsc, and electron-builder
      run: npm run pack

    - name: Upload Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: nexvr-engine-installer
        path: launcher/dist-electron/NexVR Engine Setup*.exe

    - name: Upload to Release
      uses: softprops/action-gh-release@v1
      with:
        files: launcher/dist-electron/NexVR Engine Setup*.exe