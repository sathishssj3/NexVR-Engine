# Local wrangler state — holds real waitlist addresses from `pages dev`
.wrangler/
node_modules/
package-lock.json

# Plugin scratch
.claude-flow/

# Throwaway screenshots and scripts (leading underscore = temporary).
# Anchored to this directory: an unanchored `_*` also matched
# functions/_lib/, which is real source the deploy cannot build without.
/_*

# Local secrets and environment files — holds real admin credentials and API keys.
.dev.vars
.env
.env.*
*.env

# Sensitive customer data and database exports
waitlist-subscribers*.json
*.csv
*.sql
release-email-preview.html

